{
  "name": "clean-node-login-api-js",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/gftf2011/clean-node-login-api-js.git",
  "author": "Gabriel Ferraz <gftf2011@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@commitlint/config-conventional": "^14.1.0",
    "commitizen": "^4.2.4",
    "commitlint": "^14.1.0",
    "cz-conventional-changelog": "3.3.0",
    "husky": "^7.0.0",
    "jest": "^27.3.1",
    "lint-staged": "^11.2.6",
    "standard": "^16.0.4"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "scripts": {
    "test": "jest --watch --silent --verbose --colors",
    "test:all": "jest --watchAll --silent --verbose --colors",
    "test:staged": "jest --passWithNoTests --findRelatedTests --silent",
    "test:ci": "jest --coverage --noStackTrace --silent",
    "prepare": "husky install",
    "commit": "cz"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "validator": "^13.7.0"
  }
}
